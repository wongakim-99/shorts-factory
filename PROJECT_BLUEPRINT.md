# 📑 Project Money-Printer: 기획 및 요구사항 명세서 (Mac Ver.)

## 1. 프로젝트 개요
- **목표:** 주식 커뮤니티 여론을 분석해 '나비효과 경제 분석' 쇼츠 영상 자동 생성
- **핵심 가치:** 단순 팩트 전달이 아닌, 시청자의 공포와 호기심을 자극하는 시니컬한 분석
- **운영:** 1일 1회 배치 실행 -> 영상 5개 생성 -> 인간 검수 후 1개 업로드

## 2. 상세 프로세스 (Pipeline)

### Phase 1: 데이터 수집 (Crawler)
- **Target:** DC인사이드 미국 주식 갤러리 '개념글'
- **Action:** 제목/본문/추천수 수집 -> MongoDB Upsert
- **Tech:** BeautifulSoup4, requests (User-Agent 설정 필수)

### Phase 2: 대본 작성 (Writer)
- **Engine:** Claude 4.5 Sonnet (Anthropic API)
- **Prompt Persona:** "시니컬한 경제 유튜버". ~했다체 금지, 구어체/음모론적 어조 사용.
- **Structure:** 후킹(5초) -> 팩트+뇌피셜(35초) -> 팩폭 결론(10초)

### Phase 3: 영상 생성 (Producer)
- **Engine:** MoviePy, ImageMagick (Homebrew 설치 버전)
- **Assets:**
  - 배경: `assets/video/bull.mp4` (상승), `assets/video/bear.mp4` (하락)
  - 자막: `assets/fonts/NanumGothicBold.ttf` (맥 시스템 폰트 대신 로컬 파일 사용)
  - 오디오: OpenAI TTS (or gTTS) + 배경음악 합성

## 3. 폴더 구조 (Directory Structure)
shorts-factory/
├── app/
│   ├── assets/          # 리소스
│   │   ├── video/       # 배경 영상 (bull.mp4, bear.mp4)
│   │   ├── fonts/       # 폰트 파일
│   │   └── audio/       # 배경음악
│   ├── output/          # 출력 파일
│   │   ├── images/      # 크롤링 이미지 (날짜별 폴더)
│   │   └── videos/      # 생성된 영상
│   ├── modules/
│   │   ├── crawling/    # 크롤러
│   │   ├── llm/         # 대본 작성
│   │   └── video/       # 영상 생성
│   └── core.py
├── tests/
├── main.py
├── .env
└── requirements.txt

## 4. 핵심 규칙
- 서버/웹 프레임워크 없이 로컬 Python 스크립트로 실행한다.
- 데이터는 MongoDB(Docker)에 저장하여 중복 생성을 방지한다.
- 실행은 `python3 main.py` 명령어로 일괄 처리되게 한다.